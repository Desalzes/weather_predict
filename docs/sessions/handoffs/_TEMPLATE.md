# Handoff — YYYY-MM-DD HH:MM — <slug>

## Context

<What we were doing and why. 2–3 sentences. Link to the backlog
item or ADR that motivated the session if applicable.>

## What changed

- Files touched: `<path>`, `<path>`
- Commits: `<hash>` <subject>
- Branches: `<branch>` (created / merged / rebased)

## Current state

- **Working:** <what's verified to work>
- **Not working:** <what's broken or unverified>
- **Uncommitted:** <any uncommitted delta, or "clean">

## Next action

<One specific, immediately executable step. Name a file, a
function, and a specific change. Not "continue the refactor" —
"edit `src/auth/session.rs:handle_expiry` to use the new
`ExpiryPolicy` enum from ADR-0007".>

## Watch out for

- <Surprising thing discovered this session that isn't yet in
  `_learnings.md`, or that is especially relevant to the next
  action>

## ADRs touched

- ADR-NNNN — <title> (created / updated / referenced)
