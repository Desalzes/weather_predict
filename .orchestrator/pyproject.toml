[project]
name = "weather-project-orchestrator"
version = "0.1.0"
description = "Project-local orchestration wrapper for the weather repo"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
